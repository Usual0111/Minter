BOUNTERA_MODE=demo
PORT=4173
HOST=127.0.0.1
PUBLIC_ORIGIN=http://127.0.0.1:4173
# Legacy timed Farm API only; Node Hub defaults are in assets/app-config.js.
# Existing legacy sessions retain their terms. One CR = 100 cents.
FARM_DURATION_MS=14400000
FARM_REWARD_CENTS=20
# Production requires HTTPS, a persistent disk and real provider adapters.
# BOUNTERA_MODE=production
# TELEGRAM_BOT_TOKEN=
# AD_PROVIDER=
# PROVIDER_CALLBACK_SECRET=
# DATABASE_PATH=/persistent/bountera.sqlite
