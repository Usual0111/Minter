{
  "name": "bountera",
  "version": "4.1.0",
  "private": true,
  "description": "Bountera cyan Network Hub prototype with interactive Node Hub",
  "scripts": {
    "start": "node --env-file-if-exists=.env server/index.cjs",
    "build": "node scripts/build-static.cjs",
    "test": "node tests/run.cjs"
  },
  "engines": { "node": ">=24" }
}
